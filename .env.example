# CDP Testing Configuration
# Copy this file to .env and adjust as needed

# CDP Port Configuration
# - 9222: Live mode (visible browser, gchrb-dev)
# - 9223: Headless mode (no window, gchrb-dev-headless)
CDP_PORT=9222

# CDP Mode
# - live: Visible browser window
# - headless: Background mode, no window
CDP_MODE=live

# CDP Host (usually localhost)
CDP_HOST=localhost
