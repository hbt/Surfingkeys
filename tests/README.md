
# // TODO(hbt) NEXT [tests] fix/review old tests. some could be worth migration (nvim). enhance the ones relying on mocks
# // TODO(hbt) NEXT [tests] enhance the headless tests to reuse chrome instance? (maybe) or add a flag when a test requires a clean chrome profile
